# Clean Bee Instagram — Memory

## Proposed instruction changes (pending Bee's review)
Rule-level proposals only — see CLAUDE.md → "Self-improvement". Each entry: date, which file/rule, proposed new wording, why. Bee applies or deletes the entry; the routine never edits SKILL.md/pillars.md/brand.md/config.json itself from this section.

*(none yet — first candidates will land here once a pattern repeats 3+ times)*

## Voice
- Russian, simple everyday words, informative first (see brand/brand.md, taken from the 11-09-2026 brand guideline PDF).

## Process
- 14-09-2026 (Bee): **4 posts/day, fully automatic (no approval)**, each at the peak hour of its window on Metricool's heat map; every post informative, on-brand, CTA to cleanbumblebee.com (switch config `cta` to the app once it's in the App Store).
- Next day filled at 21:00 Astana by the cloud routine.

## Projects
- **AI Reels pipeline (built 14-09-2026, LIVE from 15-09-2026):** `skills/daily-reel/` (SKILL.md, `endcard.py`, `publish_media.sh`), `reels/` (prompts, formats, trends, log.csv), config `reels` + `trends`. Own cloud routine **trig_01CxdQDFzNYsyueApDDaZQsP** "Clean Bee — AI Reel", cron `30 16 * * *` UTC = 21:30 Astana, **enabled 15-09-2026**, Apify connector attached. Settings: Seedance 2.5, 1080p, one Reel every 5 days, posted live (the 3 A/B/C drafts were the approval round) — first possible Reel 20-09-2026. Bee is on Higgsfield Plus (1,000 credits/month). To pause: `reels.enabled` false + push, or RemoteTrigger update `{enabled: false}`.
  - **Bee, 15-09-2026:** Seedance 2.5 is the Reel model ("i like seedance 2.5 more for sure"). Props should carry the Clean Bee logo when possible — done through a logo Element and a branded prop still; branded service items (bags, covers, wrap, uniforms) still off-limits because they don't exist.
  - **A/B/C test done 15-09-2026** (Bee on Plus; 221 credits): same prompt + the same 3 Nano Banana Pro stills (Bee from his Element, gold aviators, pastel lemonade can). **Seedance 2.5 won** — face clearly Bee, outfit and props exact, every beat lands, and it accepted the AI still of Bee (no real-face refusal). Cinema Studio 3.0: props and physics excellent, but the face is barely visible and the beats are rushed. Kling 3.0: camera moves right, but the face became a generic model and the outfit and can drifted. Drafts (Instagram only, not published): 15-09 18:30 Kling `375900870`, 19:00 Seedance `375902470`, 19:30 Cinema Studio `375902517`.
  - **Transparent logos on props (Bee, 15-09-2026 — "use the transparent version of the logo, so that it blends with any of the props background"):** no transparent full logo existed (only the blue circle badge and the 152 px mascot), so two cut-outs were made from the official files: `brand/logos/stacked-centred-transparent.png` (navy lettering, light surfaces — Element `cb-logo-transparent` `e669ecfd-37e7-40ea-a2e8-6bd7404fc182`) and `brand/logos/horizontal-inverse-transparent.png` (white lettering, from the official inverse logo, dark surfaces — Element `cb-logo-transparent-dark` `12f98190-543c-4eb6-ab06-043f5cd6e602`). Tested with Nano Banana Pro (4 credits): pastel lemonade can → navy logo printed straight onto the pattern; matte black cup → white logo printed straight on. Spelling and bee exact, no box in either. On the cup the model rearranged the horizontal logo into the stacked layout (bee above the lettering) — acceptable, same arrangement as the circular badge. Retired: Element `cb-logo` `ccff7494-8cd1-485e-be1a-cd7f4c536c31` (white-background PNG) and the "white rounded panel" prompt — Bee doesn't want a box, panel or sticker behind the logo.
  - Facts from the test: Metricool accepts commit-pinned raw.githubusercontent.com MP4 links (served as `application/octet-stream`) and copies them to static.metricool.com; Higgsfield video results come from the already-allowlisted `d8j0ntlcm91z4.cloudfront.net`; Seedance 2.5 outputs HEVC (endcard.py re-encodes to H.264); every model first answered with a preset recommendation ("IN THE DARK") — resubmit with `declined_preset_id`; Seedance 2.5 = 90 credits at 1080p, 65 at 720p.
  - Trend scrapers tested 14-09: apidojo TikTok search returns fresh AI-creator posts but modest views (400–50k), and with several keywords `maxItems` fills from the first keyword only → one run per keyword. data-slayer IG search "ai video" returns a mix incl. old non-AI viral reels → filter by date and judge the format.
  - `endcard.py` tested locally on 720×1280@24 with audio and 1080×1920@30 silent: +1.67 s total, card silent. `publish_media.sh` tested against a local bare repo: one commit on `media`, newest N kept, `main` untouched. Neither has run in the cloud yet — first real run will show whether the video CDN domain needs adding to the cloud network allowlist.
- **Stories (14-09-2026):** 5 reusable stories in `stories/` (see its README). 69 scheduled live for 15-09 → 28-09 by hand. **On autopilot:** the nightly routine runs `skills/story-rotation/SKILL.md` first and keeps 7 days ahead filled from `stories/schedule.json` (−25% story drops out 01-01-2027 via the schedule).
- **Clean Bumble Bee opens 28-09-2026** (Bee). Bio in `brand/instagram-bio.md` — swap to the "after opening" version that day. Live offers 14-09: −25% on everything until 31.12, first order free pickup/delivery, then free from 10 000 ₸ (500 ₸/trip otherwise).
- Public repo bayazbayev4-arch/clean-bee-instagram (images served from raw.githubusercontent.com). Cloud routine **trig_01BPXHcuLGJnHNtob7Jbykmv**, cron `0 16 * * *` UTC = 21:00 Astana.
- 15-09-2026: 4 live posts made by hand from the local session (Metricool ids 375498575, 375499113, 375498777, 375498902).
- **Routine blocked (first run 14-09-2026):** (1) `git push` 403 — the Claude GitHub App was never installed on Bee's GitHub account; (2) the cloud environment's network blocked the Higgsfield CDN and cleanbumblebee.com.
  - (2) FIXED 14-09-2026: Default cloud environment → Network access **Custom**, allowed `d8j0ntlcm91z4.cloudfront.net` + `cleanbumblebee.com`, "include default list" kept on (keeps the Trusted list; GitHub and MCP traffic bypass the allowlist anyway).
  - (1) FIXED 14-09-2026: Claude GitHub App installed (installation 161596437), repository access = only `clean-bee-instagram`. Bee did GitHub's email verification.
- **14-09-2026 one-off test run (Bee's request):** target date forced to 16-09-2026 instead of tomorrow, everything else normal. First end-to-end proof from the cloud session: git push, Higgsfield fetch, cleanbumblebee.com fetch, raw GitHub 200 checks and Metricool scheduling all worked with no blockers — the (1)/(2) fixes above hold. Story rotation: all 7 days (15-09 → 21-09) already had their 5 slots each from the earlier by-hand scheduling — 0 added. Feed: 4 posts scheduled live for 16-09-2026 (Metricool ids 375585971, 375586019, 375586070, 375586096) — new ideas (coffee stain, care-label circle symbol, bag-code/photo/online-status transparency, coats+puffer before heating season), since the pillar ideas already used were 15-09's (30-day repeat rule). Heat map was still all zero → fallback times again. One redo: `layout fact` with a literal "○" character in `--sub` renders as a tofu box — the Manrope subsets have no glyph for it and there's no fallback face; describe it in words ("значок-кружок") instead of using the Unicode circle character.
  - Runs on 14-09 and 15-09 at 21:00 will find 15-09 / 16-09 already full and add no feed posts. First normal run that makes feed posts: **16-09-2026 21:00 → fills 17-09** — check that log.
  - Fixed after the test: post b (16-09) said the circle symbol means "dry clean only, never wash at home" — wrong. Circle = professional cleaning allowed; home washing is the washtub symbol. Image + caption corrected and the Metricool post updated (new id 375589063). When replacing an image at the same path, give Metricool the commit-pinned raw URL (`.../<commit-sha>/posts/...`) — the `main` URL can serve the cached old file for a few minutes.

- **TikTok (Bee, 14-09-2026): feed posts only, stories stay Instagram-only.** Each feed post gets a separate TikTok-only Metricool post (same caption and time, **JPEG image**; `tiktokData` in SKILL.md); the nightly routine does it from 17-09.
  - **15-09-2026: TikTok rejects PNG.** The 08:30 and 12:30 TikTok posts failed in Metricool with "The content format of the Tiktok photo is incorrect. The 'image/png' type is not allowed, use 'image/jpeg' or 'image/webp' instead." Instagram takes PNG fine, and the error shows the TikTok account does publish through the API, so it isn't a personal-account problem. Fix: JPEG twins `posts/*/image.jpg` (quality 92, 4:4:4) — `compose.py` now writes one with every post, config `tiktok_image_url_pattern`, SKILL steps 6–7 use it. All 8 TikTok posts switched to the JPEG (Metricool stores it as `.jpeg`); new ids: 15-09 14:00 duvet 376144135 and 18:45 myth 376144179 (the two failed ones, re-timed), 17:00 376144230, 20:30 376144290; 16-09 08:30 376144424, 12:30 376144570, 17:00 376144726, 20:30 376144868. `updateScheduledPost` works on a post in ERROR status and puts it back to PENDING. **Verified 15-09 14:06:** the 14:00 duvet post published on TikTok with the JPEG (https://www.tiktok.com/@cleanbumblebee/video/7685685204819676437) — the account's first TikTok post that went out.
- **14-09-2026 21:00 nightly routine (first real scheduled firing):** container started with local `main` 6 commits behind `origin/main`'s actual tip (HEAD was detached at the newer commit instead). Turned out **origin/main already had the 16-09 test-run commits** (images, TikTok skill update, memory) — the local `main` branch ref just hadn't been fast-forwarded to match. Ran `git checkout main` then `git merge --ff-only <detached-tip>` + `git push origin main`; confirmed with `git ls-remote origin main` and `curl` 200s on both 15-09 and 16-09 image URLs. No content was lost, but **future runs should confirm `git branch`/`git status` isn't on a detached HEAD, and that local `main` matches `origin/main`, before doing anything** — a stale local branch ref could otherwise make PREFLIGHT's `git push --dry-run` misleading.
  - Stories: all 7 days (15-09 → 21-09) already had their 5 slots each — 0 added, nothing to do.
  - Feed: target date (tomorrow) = 15-09-2026, already had its 4 Instagram posts + 4 TikTok copies from the by-hand session — already done, nothing to add. Next date this routine will actually create posts for: **17-09-2026** (filled by the 16-09 21:00 firing), since 15-09 and 16-09 are both already full.

## Output
- 1080×1080 PNG (guideline Instagram feed spec). Layouts `tips` / `fact` / `photo` in compose.py; official logo on top, Astana chip, Safe Blue CTA pill at the bottom.
- Fonts: Manrope subsets copied from the Clean Bee site build (`.next/static/media`) — cyrillic, latin, latin-ext; Montserrat only as a last-resort glyph fallback.

## Tools
- Metricool MCP — brand `cleanbumblebee` id **6957992** (also Facebook + TikTok). Tools: getBrandSettings, getBestTimeToPostByNetwork (= the heat map), getScheduledPosts, createScheduledPost (`draft` flag in info), updateScheduledPost. Media = public URLs only; Metricool copies them to static.metricool.com on creation.
- Metricool brand timezone is Europe/London — always pass Asia/Almaty explicitly.
- **Approving or updating a Metricool post changes its `id`** (uuid stays). Re-read ids with getScheduledPosts before updateScheduledPost.
- 14-09-2026: heat map all zeros (new account) → fallback times 08:30 / 12:30 / 17:00 / 20:30.
- Higgsfield `gpt_image_2_5`, aspect 4:3 for photo backgrounds; always Read the result — the first curtains prompt produced a room with no visible curtains, so match the headline to what the photo actually shows.
